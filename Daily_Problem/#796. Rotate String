class Solution {
public:
    bool rotateString(string s, string goal) {
        int n = s.length();
        if(n!=goal.length()) return false;
        string ch = goal+goal;
        if(ch.find(s)!= -1){
            return true;
        }
        return false;
    }
};
